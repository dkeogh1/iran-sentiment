apiVersion: batch/v1
kind: Job
metadata:
  name: iran-distill-opus-mixed
  namespace: iran-sentiment
  labels:
    app.kubernetes.io/name: iran-sentiment
    app.kubernetes.io/component: distill-opus-mixed
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 14400
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iran-sentiment
        app.kubernetes.io/component: distill-opus-mixed
    spec:
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/hostname: dkbl2
      runtimeClassName: nvidia
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        runAsNonRoot: true
      containers:
        - name: distill-opus-mixed
          image: iran-sentiment
          imagePullPolicy: IfNotPresent
          args: ['stance-distill', '--recipe', 'deb-128-1e5-3', '--label-col', 'score_opus', '--fit-all', '--with-replies']
          envFrom:
            - secretRef:
                name: iran-sentiment-env
                optional: true
          env:
            - name: IRAN_DATA_DIR
              value: /data
            - name: HF_HOME
              value: /data/hf
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: "2"
              memory: 6Gi
            limits:
              memory: 20Gi
              nvidia.com/gpu: 1
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: iran-sentiment-data
